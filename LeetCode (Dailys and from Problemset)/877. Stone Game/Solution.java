class Solution {
  public boolean stoneGame(int[] piles) {
    return true;
    // int sumEven = 0, sumOdd = 0;
    // boolean isEven = true;
    // for (int i = 0; i < piles.length; i++) {
    //   if (isEven) sumEven += piles[i];
    //   else sumOdd += piles[i];
    //   isEven = !isEven;
    // }
  }
}